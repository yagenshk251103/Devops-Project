# DevOps Project Work 2

## 📌 Task: Create No-Login User


## 👤 User Details
- Username: `yagnesh`

---

## ⚙️ Step 1: Create No-Login User

```bash
sudo useradd -m -s /usr/sbin/nologin yagnesh

To verify
cat /etc/passwd | grep yagnesh

Uses
Service accounts (Apache, Nginx, MySQL)
Security hardening
Prevent human login access
Background system processes
⚠️ Important Notes
Shell path must be: /usr/sbin/nologin
User cannot login via SSH or terminal
Used only for system/service accounts
